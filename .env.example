# Copy to .env.local for local dev, or set as Vercel environment variables.
# When both are set, the config panel is hidden and no manual key entry is needed.

VITE_CE_URL=https://neoke-consent-engine.fly.dev
VITE_CE_API_KEY=your_ce_api_key_here
